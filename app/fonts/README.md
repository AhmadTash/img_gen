Place a handwritten-style TTF font here named `handwritten.ttf`.

This repo doesn't ship a font binary to avoid licensing issues.

Good sources:

- Google Fonts (look for handwriting styles)

Once you have a font file:

- copy it to `app/fonts/handwritten.ttf`
