# Web UI (React)

Tiny UI for the FastAPI `/generate` endpoint.

## Dev

Run backend at `http://127.0.0.1:8000`.

Then in this folder:

```bash
npm install
npm run dev
```

The Vite dev server proxies `/generate` and `/health` to the backend.
